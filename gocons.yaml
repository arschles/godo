bootstrap:
  commands:
    - glide install
    - sh -c 'echo "hello"''
build:
  output: "gocons"
install:
  to: $GOPATH/bin/gocons
others:
  - name: clean
    command: rm ./gocons && rm $GOPATH/bin/gocons
