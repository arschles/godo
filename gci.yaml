version: 0.0.1
build:
  output-binary: gci
  cross-compile: false
  env:
    - GOOS=darwin
    - GOARCH=amd64
test:
  paths:
    - ./actions/...
    - ./config/...
    - ./dockutil/...
    - ./log/...
    - ./tpl/...
    - ./util/...
    - .
docker:
  image-name: quay.io/arschles/gci
  tag: 0.0.1
  build:
    context:
      dir: .
      skip:
        - vendor
  push:
    auth-file-location: ~/.docker/config.json
ci:
  server:
    host: 0.0.0.0
    port: 8084
